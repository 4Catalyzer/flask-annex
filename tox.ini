[tox]
envlist = py{27,35}-{base,s3}

[testenv]
usedevelop = True

deps =
    flake8
    mock
    pytest
    pytest-cov

    s3: moto
    s3: requests

commands =
    s3: pip install -e .[s3]

    flake8 .
    pytest --cov
